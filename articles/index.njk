---
layout: base
tags: null
pagination:
    data: collections.article
    size: 10
---

<div class="max-w-lg mx-auto space-y-3">
    {% for article in pagination.items %}
        <p>
            <a href="{{ article.url }}">{{ article.data.title }} on {{ article.data.date|formattedDate }}</a>
        </p>
    {% endfor %}
</div>
