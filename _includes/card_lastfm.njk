<div class="w-full sm:w-1/2 max-w-sm mx-auto sm:px-4" x-data="lastfm()">
    <div class="px-4 py-4 h-full border border-white transition shadow-md hover:shadow-lg backdrop-blur bg-red-200/90 hover:bg-red-200 rounded">
        <h4 class="mb-4 text-xl text-red-500 text-center uppercase leading-none">letzter Scrobble</h4>
        <hr class="mb-4 border-white">
        <a class="inline-block mb-2 tracking-wide transition opacity-50 hover:opacity-75" href="https://www.last.fm/de/user/timmotheus">@timmotheus</a>
        <div class="flex items-center -mx-3">
            <div class="w-24 px-3">
                <template x-if="albumart">
                    <img alt="Album art" :src="albumart">
                </template>
            </div>
            <template x-if="artist">
                <div class="px-3">
                    <span x-text="artist"></span> – <span x-text="track"></span>
                </div>
            </template>
        </div>
    </div>
</div>
