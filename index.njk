---
layout: homepage.njk
---
{% from 'macros.njk' import heading, cv_item %}
<div class="space-y-12 max-w-lg mx-auto text-center">
    <div>
        {{ heading('h1', '👋 Hello world!') }}
        <p class="leading-relaxed">
            I’m Tim, working in a digital agency, fascinated by web development and currently focusing on developer mentoring, process optimisation and
            everything in
            between.
        </p>
    </div>

    <div>
        {{ heading('h2', 'Blog') }}

        <ul>
            {% for post in collections.post %}
                <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
            {% endfor %}
        </ul>
    </div>

    <div>
        {{ heading('h2', 'Where to find me online') }}
        {% for contact_item in contact_items %}
            <div>
                <a class="inline-block mb-2 transition hover:text-gray-700 border-b border-blue-400 hover:border-blue-600" href="{{ contact_item.url }}">
                    {{ contact_item.title }}
                </a>
            </div>
        {% endfor %}
    </div>

    <div>
        {{ heading('h2', 'My career') }}
        {% for item in cv_items %}
            {{ cv_item(item.title, item.date, item.employer, item.content) }}
        {% endfor %}
    </div>
</div>